# Implementation

I agree with your design as you outlined in `DESIGN-it-1.md`. Proceed!

For the directory that you work in, I give you full read and write permissions. Don't ask me whether you may read or modify files here.

Write the program in Golang. After each change, ensure that the sources compile and can be run, that `go.mod` is up to date, etc.. Run the appropriate `go ...` commands (such as `go mod init`, `go mod clean`) yourself and don't ask for confirmation. Also you can run `go doc ...` without confirmation.
