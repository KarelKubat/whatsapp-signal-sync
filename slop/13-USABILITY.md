# Usability

Modify the onboarding workflow that links groups. Besides `s` for skip and `d` for done (and numbers to link), add an entry `l` to re-display the list the Signal groups, after which the workflow must again prompt to determine what to do with the WhatsApp group at hand.
