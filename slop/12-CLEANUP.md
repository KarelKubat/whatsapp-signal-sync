# Cleanup

Make a directory `slop/` and move all *.md files into it, except for `README.md`. That includes this file `12-CLEANUP.md`. 

Since we are in a Git repository, use `git mv` and not just `mv`.
