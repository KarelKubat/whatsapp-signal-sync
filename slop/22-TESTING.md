# Testing

A photo on Signal gets correctly forwarded to WhatsApp so that works. But I received a pic without a message, and the WhatsApp reads:

```
[Signal Direct: +41792976806] Annemieke Meiborg: 
```

When there is no description, don't show the traling `:`.
