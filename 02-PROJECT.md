# Additional requirements

I have reviewed `DESIGN-it-0.md` and I have the following comments.

- You can look at existing CLI programs that perform similar tasks, such as `signal-cli`. However, when you will implement the program, do not rely on the existence of such external utilities. I want the program to be self-contained without external dependencies.

- With this information, re-design the program and write your design in file `DESIGN-it-1.md`.
